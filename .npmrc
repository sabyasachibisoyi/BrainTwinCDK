# Force `npm install <pkg>` to write exact versions (no carets) into
# package.json — same discipline as the Python side (requirements.txt
# pinned with ==). Reproducible builds across local + cloud.
save-exact=true
